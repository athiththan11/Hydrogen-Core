'use strict';

let _environmentConfs = {
	_name: 'Production and Sandbox',
	type: 'hybrid',
	'api-console': true,
	_description: 'This is a hybrid gateway that handles both production and sandbox token traffic.',
	_hostname: 'localhost',
	offset: 0,
};

exports._environmentConfs = _environmentConfs;
